# frp 服务端（跑在轻量云 VPS）

bindPort = 7000

[auth]
method = "token"
token = "改成长随机字符串"

# 可选：dashboard
# webServer.addr = "127.0.0.1"
# webServer.port = 7500
# webServer.user = "admin"
# webServer.password = "改成密码"
